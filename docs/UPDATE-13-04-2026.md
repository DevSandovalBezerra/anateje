# UPDATE - 13-04-2026

## Resumo

Este update registra o avanço da migração para a arquitetura modular (API v2), incluindo o bootstrap do kernel PSR-7/15, contratos + adapters para integração com o legado e a criação inicial do `events-module` (ainda em fase de alinhamento com o padrão de módulos do v2).

## Entregas principais

1. Entrada da API v2 + roteamento:
   - `api/v2/index.php`
   - `.htaccess` (regra para `api/v2`)

2. Kernel / Container / ResponseFactory para o v2:
   - `src/Http/Kernel.php`
   - `src/Http/ResponseEmitter.php`
   - `src/Http/NyholmResponseFactory.php`
   - `src/Container/Container.php`
   - `bootstrap/modula.php`

3. Registro de módulos do v2:
   - `config/modules.php`

4. Contratos e adapters para interoperar com o legado:
   - `modules/contracts/src/*`
   - `src/Adapters/*`

5. Módulos migrados (v2):
   - `modules/health-module`
   - `modules/audit-module`
   - `modules/permissions-module`
   - `modules/integrations-module`
   - `modules/members-module`
   - `modules/benefits-module`

6. Eventos — início da migração (ainda não integrado ao padrão do v2):
   - `modules/events-module/src/Application/Helpers.php`
   - `modules/events-module/src/Http/Handlers/*`
   - `modules/events-module/src/Module.php`

7. Breakpoint report para continuidade de Eventos:
   - `docs/BREAKPOINT-REPORT-MIGRACAO-EVENTOS-2026-04-13.md`

8. Suite de testes (ampliação do discovery):
   - `phpunit.xml` (inclui `modules/*/tests`)

9. Composer (path repositories para módulos):
   - `composer.json`
   - `composer.lock`

## Observações técnicas

- O `events-module` ainda está divergente do padrão dos módulos do v2 (os módulos atuais expõem `RouteProvider` + `Route[]` e o Kernel consome apenas essas rotas). O detalhamento e o passo a passo de correção estão no breakpoint report:
  - `docs/BREAKPOINT-REPORT-MIGRACAO-EVENTOS-2026-04-13.md`

