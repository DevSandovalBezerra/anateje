# UPDATE - 05-03-2026

## Resumo

Este update consolida a base inicial de testes automatizados com PHPUnit, inclui validacoes de upload/autenticacao, registra o script de limpeza de contaminacao do banco do LiderGest e versiona os pendentes locais solicitados.

## Entregas principais

1. Infra de testes com PHPUnit:
   - `composer.json`
   - `composer.lock`
   - `phpunit.xml`
   - `tests/bootstrap.php`

2. Runner de API para testar endpoints que encerram com `exit`:
   - `tests/Support/ApiRunner.php`
   - `tests/support/run_endpoint.php`

3. Testes iniciais implementados:
   - `tests/Unit/BootstrapValidationTest.php`
   - `tests/Integration/ApiAuthTest.php`
   - `tests/Integration/MemberFoldersUploadValidationTest.php`

4. Ajustes de versionamento:
   - `.gitignore` atualizado para ignorar `vendor/`, `.phpunit.cache/` e `tmp/phpunit_sessions/`.

5. Script de limpeza de contaminacao (LiderGest x ANATEJE):
   - `dados/cleanup_lidergest_contaminacao_anateje_20260303.sql`

6. Registro de pendencias locais solicitadas:
   - `config/sql/migrate.php`
   - `config/sql/migrate_web.php`
   - `anasite.zip`
   - `exemplo.png`
   - `uploads/`

## Execucao de testes

Comando executado:

```bash
vendor\bin\phpunit --configuration phpunit.xml
```

Resultado:

- `OK (9 tests, 23 assertions)`

## Observacao tecnica

Persistem avisos `Deprecated` no PHP 8.5 referentes ao uso de `$http_response_header` em `api/v1/_bootstrap.php` (linhas na regiao 380/381). Nao bloqueiam a suite atual, mas devem ser ajustados no proximo ciclo.

